#ifndef __P4_PGTABLE_DEFS_H
#define __P4_PGTABLE_DEFS_H

/* x86 architecture splits page.h to pgtable_types.h. Our
 * version still uses only page.h so we provide an empty
 * pgtable_types.h used only by x86. The other architectures
 * still use page.h for all definitions
 */

#endif
